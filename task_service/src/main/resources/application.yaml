spring:
  application:
    name: task
  datasource:
    url: jdbc:postgresql://localhost:5555/spring-postgres
    username: gleb
    password: password
  jpa:
    hibernate:
      ddl-create: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        hbm2ddl:
          auto: update
server:
  port:
    8085